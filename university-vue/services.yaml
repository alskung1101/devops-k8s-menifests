apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: university-vue-rollout
  namespace: university
spec:
  type: ClusterIP
  selector:
    app: university-vue
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: university-vue-preview
  namespace: university
spec:
  type: ClusterIP
  selector:
    app: university-vue
  ports:
  - port: 80
    targetPort: 80